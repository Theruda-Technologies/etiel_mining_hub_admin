export type DashboardStats = {
  pendingCount: number;
  processingCount: number;
  failedCount: number;
  productCount: number;
  serviceCount: number;
  orderCount: number;
  pendingOrders: number;
};
