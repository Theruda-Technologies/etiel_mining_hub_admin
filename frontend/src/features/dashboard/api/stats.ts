import type { DashboardStats } from "../types";

export async function getDashboardStats(): Promise<DashboardStats> {
  // Replace with Supabase queries as tables are added
  return {
    totalUsers: 0,
    activeSessions: 0,
  };
}
